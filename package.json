{
  "name": "tracker-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "server": "cd packages/server && npm run dev",
    "client:ios": "cd packages/client && npm run ios",
    "client:android": "cd packages/client && npm run android"
  }
}
